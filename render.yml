services:
  - name: ollama-service
    type: web
    runtime: docker
    region: oregon
    plan: starter  # Use a higher plan if needed
    dockerContext: ollama
    dockerfilePath: ollama/Dockerfile
    envVars:
      - key: OLLAMA_PORT
        value: "11434"
    healthCheckPath: "/"
    autoDeploy: true
